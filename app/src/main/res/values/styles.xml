<resources>

    <!-- Base application theme. -->
    <style name="AppTheme" parent="Theme.AppCompat.Light.NoActionBar">
        <!-- Customize your theme here. -->
        <item name="colorPrimary">@color/colorPrimary</item>
        <item name="colorPrimaryDark">@color/colorPrimaryDark</item>
        <item name="colorAccent">@color/colorAccent</item>
<!--        <item name="android:textColorPrimary">@color/textBackground</item>-->
        
        <item name="windowActionModeOverlay">true</item>
        <item name="actionModeStyle">@style/ActionModeStyle</item>
    </style>


    <style name="myToolBarTheme" parent="ThemeOverlay.AppCompat.Light">
        <item name="android:textColorPrimary">@color/textColorToolBar</item>
        <item name="android:textColorSecondary">@color/threePointMenu</item>
    </style>

    <style name="itemsMenu" parent="ThemeOverlay.AppCompat.Light">
        <item name="android:textColorPrimary">@color/textColorItemsMenu</item>
    </style>

    <style name="ActionModeStyle" parent="@style/Widget.AppCompat.ActionMode">
        <item name="background">@color/colorPrimary</item>
        <item name="colorPrimary"></item>
    </style>

</resources>
